---
name: OpenWebUI - LiteLLM
version: '3.8'
services:
    openwebui:
        image: ghcr.io/open-webui/open-webui:main
        container_name: open-webui
        ports:
            - "3000:8080"
        volumes:
            # yamllint disable-line rule:line-length
            - ./data/openwebui:/app/backend/data:Z  # Remove :Z if you are not using SELinux
        extra_hosts:
            - "host.docker.internal:host-gateway"
        environment:
            - WEBUI_AUTH=False
            - OPENAI_API_KEY=${MASTER_KEY}
            - OPENAI_API_BASE_URL=http://host.docker.internal:4000/v1
        restart: always

    litellm:
        image: docker.io/litellm/litellm-database:v1.82.3-stable.patch.2
        container_name: litellm
        env_file:
            - .env
        ports:
            - "4000:4000"
        volumes:
            # yamllint disable-line rule:line-length
            - ./config.yml:/app/config.yaml:Z  # Remove :Z if you are not using SELinux
        command: --config /app/config.yaml --port 4000
        restart: always
        environment:
            # yamllint disable-line rule:line-length
            DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}"
            STORE_MODEL_IN_DB: "True"

    db:
        image: postgres
        restart: always
        volumes:
            # yamllint disable-line rule:line-length
            - ./data/postgres:/var/lib/postgresql:Z  # Remove :Z if you are not using SELinux
        environment:
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        healthcheck:
            # yamllint disable-line rule:line-length
            test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
            interval: 1s
            timeout: 5s
            retries: 10
